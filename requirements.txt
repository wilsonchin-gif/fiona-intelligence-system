# Fiona Market News shadow/image modes use Pillow for PNG rendering and validation.
# Pillow is loaded on demand; text mode and the other briefs do not render images.
Pillow==12.3.0; python_version >= "3.10"
Pillow==11.3.0; python_version < "3.10"
